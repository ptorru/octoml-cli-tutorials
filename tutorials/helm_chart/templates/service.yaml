apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.Name }}
  name: {{ .Values.Name }}
  namespace: {{ .Values.Name }}
spec:
  clusterIP: 172.20.38.211
  clusterIPs:
  - 172.20.38.211
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8001
  selector:
    app: {{ .Values.Name }}
  sessionAffinity: None
  type: ClusterIP
