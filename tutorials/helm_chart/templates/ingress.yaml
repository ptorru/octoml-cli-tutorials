apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: GRPC
  name: fortune-ingress
  namespace: {{ .Values.Name }}
spec:
  ingressClassName: nginx
  rules:
  - host: {{ .Values.Name }}.demo.octoml.ai
    http:
      paths:
      - backend:
          service:
            name: {{ .Values.Name }}
            port:
              number: 80
        path: /
        pathType: Prefix
